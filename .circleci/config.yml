version: 2

jobs:
  build:
    docker:
      - image: circleci/ruby:2.6.3-node-browsers
      
    steps:
      - checkout

      - run:
          name: Greeting
          command: echo Hello, world.
          
      - run:
          name: Print the Current Time
          command: date
