version: 2
jobs:
  build:
    docker:
      - image: circleci/ruby:2.4.1-node-browsers

    steps:
      - checkout
      
      - run:
          name: Greetings
          command: echo Hello, Word

      - run:
          name: Print the Current Time
          command: date
