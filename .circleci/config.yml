version: 2
jobs:
  build:
    docker:
      # specify the version here
      - image: circleci/ruby:2.4.1-node-browsers

    steps:
      - checkout

      # specify any bash command here prefixed with `run: `
      - run: 
          name: Greeting
          command: echo Hello , Sajjadul

      - run:
          name: Print the Current Time
          command: date
