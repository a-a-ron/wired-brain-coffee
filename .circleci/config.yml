version: 2.1
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      
      - run:
          name: Greeting
          command: echo 'Hello World!'
          command  echo 'This is the delivery pipeline'
      - run:
          name: Print the current time
          command: date
