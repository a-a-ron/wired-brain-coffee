
version: 2.1

jobs:
  build:
    docker:
      - image: circleci/ruby:2.4.1-node-browsers

    steps:
      - checkout

      - run:
          name: build dependencies
          command: bunde install

      - run:
          name: build the jekyll site
          command: bunde exec jeykll build
