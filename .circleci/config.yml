version: 2.1
jobs:
  build:
    docker:
      - image: circleci/ruby:2.4.1-node-browsers
    steps:
      - checkout
      
      - run:
          name: The First Step
          command:
            echo 'Hello World!'
            echo 'This is the delivery pipeline'
      - run:
          name: Print Current Time
          command: date
          
